{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "20"
  },
  "steps": {
    "install": {
      "inputs": [
        "."
      ],
      "commands": [
        "npm install --include=dev"
      ]
    }
  }
}